# Exception Handling

<dl>
    <dt>Exception</dt>
    <dd>
        An `exception` is an event that occurs during the `execution of a program` and disrupts its `normal flow of instructions`.Exceptions are a mechanism to handle `runtime errors` and maintain the `application's stability`. 
    </dd>
</dl>


